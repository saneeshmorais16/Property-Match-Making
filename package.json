{
  "name": "hybrid-matching-free-embeddings",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "embed": "ts-node-dev --transpile-only scripts/embed.ts",
    "match": "ts-node-dev --transpile-only scripts/match.ts --"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.3",
    "@xenova/transformers": "^2.14.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.30",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}